# AgriShield AI

Premium farm security UI — Splash → Login → Home / Live / Alerts / Analytics / Profile.

Settings & Devices open from Profile.

## Run

```powershell
$env:PATH = "C:\Users\Public\flutter\bin;" + $env:PATH
cd D:\kavach-database\agri_shield_ai
flutter pub get
flutter run -d chrome
```

Press **R** after UI changes.
